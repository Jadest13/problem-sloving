#include <iostream>
#include <algorithm>

using namespace std;
using LL = long long;

LL N;

int main() {
  ios_base :: sync_with_stdio(false);
  cin.tie(NULL);
  cout.tie(NULL);

  cin >> N;
  cout << N*N*N << "\n" << 3;
}