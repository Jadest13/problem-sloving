#include <iostream>
#include <vector>
#include <algorithm>

using namespace std;

int N;
int A[10];

int main() {
  ios_base :: sync_with_stdio(false);
  cin.tie(NULL);
  cout.tie(NULL);

  cout << !1 << "\n" << !0;

  return 0;
}