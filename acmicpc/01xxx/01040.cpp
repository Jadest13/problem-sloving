#include <iostream>
#include <vector>
#include <algorithm>

using namespace std;
using LL = long long;

string N;
int K;
int bit;

LL R;

int main() {
  ios_base :: sync_with_stdio(false);
  cin.tie(NULL);
  cout.tie(NULL);

  cin >> N >> K;
  for(int i = 0; i < 10; i++) {
    bit 
    if(R = F()) {
      break;
    }
  }

  cout << R;

  return 0;
}